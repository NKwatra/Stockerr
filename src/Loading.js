import React from 'react';

/*
    This component is used to show loading indicator when the stocks are loading
*/
export default () => {
    return (
        <div className="loading mx-auto"></div>
    )
}